<!--
  ~ Copyright (c) 2015. bysy@users.noreply.github.com
  -->

<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
    <style name="SettingsTheme" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Custom notifications need to have styles set for text to appear correctly on different APIs.
         http://stackoverflow.com/a/4935191 -->
    <style name="notificationTitle" parent="@android:style/TextAppearance.StatusBar.EventContent.Title"/>
    <style name="notificationText" parent="@android:style/TextAppearance.StatusBar.EventContent" />

</resources>
